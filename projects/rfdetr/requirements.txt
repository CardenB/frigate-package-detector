# RF-DETR project deps — install in projects/rfdetr/.venv (NOT the main venv;
# DETR/transformer deps conflict with the Ultralytics stack). See setup.sh.
rfdetr>=1.1.0
pyyaml>=6.0
onnx>=1.16
onnxruntime-gpu>=1.18
# torch is pulled by rfdetr; for the 5090 (Blackwell) install the cu128 build
# FIRST (see setup.sh) so it isn't overwritten by a default wheel.
